#!/usr/bin/python

from distutils.core import setup

setup('consensus_caller',
  version='0.1',
  description='Command line consensus genotype caller for exome sequence
experiments',
  author='Vassily Trubetskoy',
  )
